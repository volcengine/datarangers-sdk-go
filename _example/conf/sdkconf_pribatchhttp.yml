sdk:
  mode: http
#  logLevel: debug
http:
  addr: ${SDK_DOMAIN} #上报的IP 或 域名
#  timeout: 30 # secenods
batch:
  enable: true
#  size: 20
#  waitTimeMs: 100
#asyn:
#  routine: 20   #建议小于1024，并发数目
#  queueSize: 10240